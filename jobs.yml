jobs:
  - name: fetch-image-job-100
    type: manifest
    steps:
      - GET: dockimage-11
      - GET: dv-opts


  - name: fetch-cluster-job-101
    type: gkeDeploy
    steps:
      - GET: fetch-image-job-100
      - GET: gke-cluster-11
      
  - name: fetch-cluster-again
    type: gkeDeploy
    steps:
      - GET: fetch-cluster-job-101
      - GET: gke-cluster-11
