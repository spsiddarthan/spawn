language: java
services:
  - mysql
  - postgres
  - redis
  - memcache
  - elasticsearch
  - mongodb
  - rabbitmq
  - neo4j
  - couchdb
  - rethinkdb

build:

  pre_ci_boot:
    image_name: drydock/u14javpls
   # image_name: shippable/minv2
    image_tag: latest
    pull: true
    
  ci:
    - ps -eax
    - ls -al
    
