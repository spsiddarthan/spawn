language: java
services:
  - postgres
  
build:

  pre_ci_boot:
    image_name: drydock/u14nodpls
   # image_name: shippable/minv2
    image_tag: prod
    pull: true
    
  ci:
    - ps -eax
    - ls -al
    
